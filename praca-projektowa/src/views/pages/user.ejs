<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>User: <%= user.username %></title>
  </head>
  <body>
    <h1>User</h1>

    <% if (user) { %>
      <p><strong>Username:</strong> <%= user.username %></p>
    <% } else { %>
      <p>User not found.</p>
    <% } %>

    <p><a href="/">Back</a></p>
  </body>
</html>